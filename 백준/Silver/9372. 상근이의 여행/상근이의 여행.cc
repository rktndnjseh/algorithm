#include <iostream>

using namespace std;

int main() {
    int T;

    cin >> T;
    while (T-- > 0) {
        int N, M;
        cin >> N >> M;
        int a, b;
        while (M-- > 0) {
            cin >> a >> b;  
        }
        cout << N - 1 << endl;
    }
    return 0;
}